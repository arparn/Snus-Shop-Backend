
stages:
  - build
  - test
  - deploy

before_script:
  - export GRADLE_USER_HOME=`pwd`/.gradle

build snus_shop-backend:
  stage: build
  cache:
    paths:
      - .gradle/wrapper
      - .gradle/caches
  artifacts:
    paths:
      - build/libs
  tags:
    - snus_shop-backend
  script:
    - ./gradlew assemble

test snus_shop-backend:
  stage: test
  tags:
    - snus_shop-backend
  script:
    - ./gradlew check

deploy snus_shop-backend:
  stage: deploy
  only:
    refs:
      - master
  tags:
    - snus_shop-backend
  script:
    - mkdir -p ~/api-deployment # mkdir make folder api-deployment ~/ is under current user directory so for gitlab it would be /home/gitlab/api-deployment
    - rm -rf ~/api-deployment/* # rm remove -rf is recursive files from api-deployment
    - cp -r build/libs/. ~/api-deployment # cp - copy build/libs is where

